<Window
    x:Class="CustomRDPInstaller.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CustomRDPInstaller"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid x:Name="LogoGrid" Grid.Row="0">
            <StackPanel />

        </Grid>
        <Grid x:Name="HomeScreenGrid" Grid.Row="1">
            <StackPanel />
        </Grid>
        <Grid x:Name="InstallingGrid" Grid.Row="1">
            <StackPanel />
        </Grid>
        <Grid x:Name="ClosingGrid" Grid.Row="1">
            <StackPanel />
        </Grid>
    </Grid>
</Window>
